apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: http-hello-world-dev
  namespace: z9874dc1d-argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argo.octopus.com/project: "test"
    argo.octopus.com/environment: "test"
spec:
  project: http-hello-world

  source:
    repoURL: https://github.com/fabienfleureau/argocd-test-http-server.git
    targetRevision: main
    path: overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
